const config = {
        mongodbUrl: "{{ bc_haya_metrics_server_mongodb_url }}",
        dbUpdatePeriod: 5000,
        hayaNodeUrl: "{{ bc_haya_metrics_server_haya_node_url }}",
        hayaUpdatePeiod: 1000,
        baseToken: "BET",
        tokenAccount: "eosio.token"
};

module.exports = config;
